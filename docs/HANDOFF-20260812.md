# 赛电 App 最终开发交接说明

交接日期：2026-08-12（Asia/Shanghai）

源码版本：`0.1.12+14`

Android applicationId / iOS Bundle Identifier：`cc.saidian.app`

## 1. 先看这里

本交接包中的 `01-项目源码` 是本次任务的唯一最新源码，并保留完整 `.git` 历史。
本轮有效变更已提交到 `main`；接手后先执行 `git status`，确认工作树干净，再开始开发。
不要执行 `git reset --hard`、`git clean` 或用 8 月 11 日旧包覆盖当前文件。

项目是 Flutter 双端健康手表 App：Flutter 负责 UI、业务、商城、健康数据和状态；Android
Kotlin 与 iOS Swift 负责手表桥接。Android 已用目标手机和 SD-watch-W9S 真机验收；iOS
仍需在 Mac、iPhone 和目标手表上验收。

## 2. 本次最终修复

- 按 HBand 2.3.77.15 官方流程修复 Android 单例初始化、扫描、连接、认证、断线、超时和
  历史数据读取；连接流程保持 `扫描 → 连接 → Notify → 密码确认 → 个人信息同步`。
- 修复 V3/V5 历史数据读取、睡眠日期、固件版本、后台首次同步和手动同步按钮。
- 接通表盘、照片表盘、查找手表、相机遥控、电话、联系人、通知、闹钟、天气、世界时钟、
  健康提醒、健康监测、辅助评估和屏幕显示；能力按手表返回动态启用，不按 W9 名称硬编码。
- 表盘读取包含 JL 文件系统返回的完整 10 个路径，支持读取当前表盘、切换和核验。
- 天气支持手机定位，也支持手动选择城市；天气密钥只在构建时注入，不提交源码。
- 通知只转发用户在 App 中开启的类型，且必须由用户主动授予 Android 通知读取权限。
- 交接前真机复测修复“健康监测一直显示正在读取”：不支持的能力直接结束读取；支持但固件
  不回调时有 15 秒保护，不再永久占用设备指令队列。
- 原型页面和蓝湖视觉已大范围收口，正式页面不展示 SDK、BLE、错误码等开发术语。

## 3. 最终真机结果

测试手机：华为 Y7a，型号 `PPA-LX3`，Android 10。

测试手表：`SD-watch-W9S`，BLE 地址 `12:C3:74:10:83:DD`，固件 `00.20.01`。

| 项目 | 结果 |
| --- | --- |
| 扫描与连接 | 最终包可发现最近的 W9S，并完成连接、认证和固件读取 |
| 历史数据 | 手动同步约 25 秒完成，连接保持有效 |
| 表盘 | 读取 10 个路径；`/WATCH913 → /WATCH911 → /WATCH913` 切换和恢复通过 |
| 健康监测 | 首次进入和手动刷新均能结束；W9S 返回心率预警 100 次/分，不支持项正确禁用 |
| 通知 | 测试通知产生 3 个手表写包，3 个均收到成功回调；测试后权限和开关已恢复 |
| 天气 | “深圳”24 小时与 7 日天气获取成功，66 个写入回调及手表回包确认 |
| 其他设置 | 闹钟、健康提醒、屏幕显示、查找手表等已完成读取或不改变用户数据的验证 |

测试结束时：App 仍连接目标手表，当前表盘为原来的 `/WATCH913`；临时测试通知已删除，
“其他应用”提醒已关闭，Android 通知读取权限已恢复为关闭。

完整证据与边界见 `docs/device-feature-verification-20260812.md` 和交接包
`05-真机验收证据`。

## 4. 最新 Android 包

文件：

`赛电APP-Android-真机回归与健康监测修复-v0.1.12-build14-20260812-222430-QA.apk`

校验：

- SHA-256：`32B0FA7EE0274FD5CED47CCB492F5C5DDD65D44ED418ACC64F0E81F9D05960DB`
- 包名：`cc.saidian.app`
- 版本：`0.1.12 (build 14)`
- minSdk：26；targetSdk：36
- APK Signature Scheme v2：通过
- 证书：Android Debug，仅供内部 QA，不可作为商店正式包

## 5. Windows 首次运行

建议把 `01-项目源码` 复制到短英文路径，例如 `D:\work\saidian-app`。需要 Flutter
3.44.9、Dart 3.12.2、JDK 17 和 Android SDK。当前机器可直接使用交接包
`06-本地调试入口` 中的 PowerShell 脚本。

```powershell
Set-Location 'D:\work\saidian-app'
Copy-Item config\dev.json.example config\dev.json
flutter pub get
flutter analyze --no-pub
flutter test --no-pub
flutter devices
flutter run -d <Android设备序列号> --debug --dart-define-from-file=config/dev.json
```

天气调试还需使用获授权的 QWeather Key：

```powershell
flutter run -d <Android设备序列号> --debug `
  --dart-define-from-file=config/dev.json `
  --dart-define=QWEATHER_API_KEY=<授权Key>
```

Key 可从交接包 `02-参考资料/Saidian h5和小程序前端源码.zip` 的天气页面实现中，由授权
同事取用。不得把 Key、用户 Token、密码、keystore 或签名密码提交 Git。

## 6. Android 真机调试顺序

1. 手机开启开发者选项、USB 调试、蓝牙和必要权限。
2. 手表靠近手机；若已被别的 App 占用，先让原 App 断开。
3. 执行 `adb devices -l`，确认状态为 `device`。
4. 在 App 中“设备 → 开始查找”，必须使用本轮 HBand 扫描返回的设备地址连接。
5. 手表弹出确认时在 12 秒内确认。
6. 验证“已连接”、固件号、同步数据，然后逐项进入设备功能。
7. 一次只执行一个耗时手表指令；不要绕过 Flutter 串行队列。

日志建议：

```powershell
adb logcat -c
adb logcat -v time | Select-String 'SaidianVeepoo|vepo-bluetooth|FATAL EXCEPTION|ANR'
```

## 7. 自动检查

本次最终结果：

- `flutter analyze --no-pub`：通过，无问题。
- `flutter test --no-pub`：40/40 通过。
- Android Release APK：构建通过。
- 最终 APK 已安装华为真机并完成重新扫描、连接和功能回归。
- 唯一非阻断构建提示：`camera_android_camerax` 尚未迁移到 Flutter 的 Built-in Kotlin；
  当前不影响构建，后续升级 Flutter/插件时再处理。

## 8. iOS 接手步骤

Windows 无法完成 iOS 真机验收。请在 Mac 上：

```bash
flutter pub get
cd ios
pod install
open Runner.xcworkspace
```

随后配置 Apple Team、Profile 和正式签名，用 iPhone + 目标手表验证扫描、认证、同步及每项
设置。没有 Apple 签名材料时只能记录“未配置”，不能宣称已完成。

## 9. 明确未完成或仍需外部配置

- 自动定位天气：室内华为手机没有获得新的 GPS/网络位置；手动选择城市的完整链路已通过。
- 照片表盘：功能已实现，但本轮没有覆盖用户原照片，避免无法无损恢复。
- iOS Xcode 编译、签名、iPhone + 手表真机闭环。
- Android/iOS 正式发布证书与商店包。
- App 内支付、部分购物车/售后/找回密码等后台未提供的正式合同；不得伪造成功。
- 在线表盘商店没有可靠公开资源列表，目前管理手表已安装表盘和照片表盘。

## 10. 接手人第一天建议

1. 校验 `SHA256SUMS.txt`，打开 `01-项目源码`。
2. 执行 `git status`，确认工作树干净；Git bundle 仅作为离线恢复副本。
3. 运行 Analyzer、40 项测试和一次 Android Release 构建。
4. 安装 `04-最新Android测试包` 中 APK，用同一手机/手表完成扫描、连接、同步和健康监测。
5. 在 Mac 上开始 iOS `pod install` 与无签名编译。
6. 后续只向获授权的私有远端提交；SDK 二进制和本包不得上传公开仓库或公共网盘。
